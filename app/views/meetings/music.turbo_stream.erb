<%= turbo_stream.append "meetings",
                        partial: "meetings/meeting_for_music",
                        collection: @meetings,
                        as: :meeting %>

<%= turbo_stream.replace "pager",
                         partial: "shared/pager",
                         locals: { pagy: @pagy } %>
