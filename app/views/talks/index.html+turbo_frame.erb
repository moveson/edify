<%= turbo_frame_tag :filter_and_search do %>
  <%= turbo_stream.replace "talks_and_pager", partial: "talks_and_pager", locals: { talks: @talks, pagy: @pagy } %>
  <%= turbo_stream.replace "talk_count", partial: "talk_count", locals: { pagy: @pagy } %>
  <%= turbo_stream.replace "sort_and_filter_dropdowns", partial: "sort_and_filter_dropdowns", locals: { q: @q } %>
<% end %>
